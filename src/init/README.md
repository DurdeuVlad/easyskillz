# src/init
**Responsibility**: Multi-step initialization flows involving `detect`, `plan`, and `execute` phases.
**Extension**: Add new initialization steps or modify existing flows to handle new project setups.
**Maintenance**: Adjust initialization logic as new detectors or execution steps are introduced.