# src/core
**Responsibility**: Base classes and shared logic for commands (e.g., `BaseCommand.js`).
**Extension**: Add shared base classes for specific command categories to provide common functionality.
**Maintenance**: Refactor common logic to prevent duplication and ensure consistency across commands.