# src/domains
**Responsibility**: High-level domain entry points (CLI router) for `skill`, `tool`, `project`, and `docs`.
**Extension**: Add a new domain file if a new major CLI category is introduced.
**Maintenance**: Update the router to point to new command subcategories or domains.