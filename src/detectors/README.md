# src/detectors
**Responsibility**: Detect if an AI tool exists in the project using specific markers.
**Extension**: Create a new file named after the tool (e.g., `mytool.js`) following the existing pattern.
**Maintenance**: Update logic if a tool changes its default configuration or detection markers.