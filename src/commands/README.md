# src/commands
**Responsibility**: Main CLI command implementations, categorized into `skill`, `tool`, `project`, and `docs`.
**Extension**: Add new command files in the relevant subfolders (e.g., `skill/NewCommand.js`).
**Maintenance**: Update command logic to reflect changes in CLI usage or core functionality.